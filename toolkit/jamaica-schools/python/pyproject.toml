[project]
name = "jamaica-schools"
version = "0.1.0"
description = "Jamaica schools and universities directory"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
